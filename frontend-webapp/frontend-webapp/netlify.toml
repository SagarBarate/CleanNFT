[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Next.js requires specific redirects for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

